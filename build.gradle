apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    compile "com.thoughtworks.gauge.maven:gauge-maven-plugin:2.0.0"
}

task copyToLib(type: Copy) {
    into "$projectDir/libs"
    from configurations.runtime
}
